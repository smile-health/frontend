import { ArgTypes, Meta, Stories } from '@storybook/blocks'

import { ModalConfirmation } from '@repo/ui/components/modules/ModalConfirmation'

import * as ModalConfirmationStories from './ModalConfirmation.stories'

<Meta of={ModalConfirmationStories} />

# ModalConfirmation

```tsx
import { ModalConfirmation } from '@repo/ui/components/modules/ModalConfirmation'
```

## ModalConfirmation Props

<ArgTypes of={ModalConfirmation} />

<Stories />
