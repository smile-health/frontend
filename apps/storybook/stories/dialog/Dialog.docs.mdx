import { ArgTypes, Meta, Stories } from '@storybook/blocks'

import {
  Dialog,
  DialogCloseButton,
  DialogContent,
  DialogFooter,
  DialogHeader,
} from '@repo/ui/components/dialog'

import * as DialogStories from './Dialog.stories'

<Meta of={DialogStories} />

# Dialog

```tsx
import {
  Dialog,
  DialogCloseButton,
  DialogContent,
  DialogFooter,
  DialogHeader,
} from '@repo/ui/components/dialog'
```

## Dialog Props

<ArgTypes of={Dialog} />

## DialogCloseButton Props

<ArgTypes of={DialogCloseButton} />

## DialogContent Props

<ArgTypes of={DialogContent} />

## DialogFooter Props

<ArgTypes of={DialogFooter} />

## DialogHeader Props

<ArgTypes of={DialogHeader} />

<Stories />
